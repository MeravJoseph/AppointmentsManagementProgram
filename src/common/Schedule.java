package common;
import java.util.Vector;

public class Schedule {
	/*
	Vector<Patient> appointments = null;
	int appLen; // in minutes. 15 for family doctor. 20 for others.
	int numOfApps; // number of doctor appointments per day.
	
	public Schedule(boolean isFamilyDoc){
		if(isFamilyDoc){
			appLen = 15;
			numOfApps = 32;
			for(int i=0; i<numOfApps; i++){
				appointments.add(null);
			}
			System.out.println(appointments.size());
		}
		else{
			appLen = 20;
			numOfApps = 24; 
			for(int i=0; i<numOfApps; i++){
				appointments.add(null);
			}
			System.out.println(appointments.size());
			// 8 hours * 3 = 24
		}
		
	}*/
}
